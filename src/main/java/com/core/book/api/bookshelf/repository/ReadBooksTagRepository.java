package com.core.book.api.bookshelf.repository;

import com.core.book.api.bookshelf.entity.ReadBooksTag;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReadBooksTagRepository extends JpaRepository<ReadBooksTag, Long> {
}
